# STATE — sbp-os (generated snapshot)

**As-of:** 2026-07-02 · round **SBP-HYGIENE-R1** (branch hygiene, dispatched off brain-os **B-147** — no site-content changes). **The live site is untouched and stays coming-soon.** This round: **(a)** the live holding page got a durable, clearly-named home — **`production-holding`** (byte-identical to `claude/busy-hamilton-jqwkgt`'s tip; **never merges to `main`**; the one operator action is repointing Vercel production to it, §O-guided, zero visible change) — and **(b)** `main`'s restoration was built and pushed **inert** on `claude/sbp-os-production-holding-h3l1ks`: PR #4's coming-soon overlay reverted (full 13-page homepage back, tree = the W8 recovery point), the W9 close docs landed, the stranded `friendly-meitner` screenshot-slots commit (B-14) folded in. **Gates open: operator Vercel repoint → audit → PR merge.** No DNS touched.

1. **⭐ STANDING LAUNCH GATE LIST (operator's five items, 2026-07-02 — brain-os B-147 / local B-24).** The site **stays coming-soon until the operator is satisfied**. To launch (= flip Vercel production from `production-holding` to `main`; no DNS change ever):
   1. **Product screenshots in action** — CRM, Dash, AMS, IC, Motion (= the queued **JCV demo-brand cross-unit session**, B-23; site slots already swap-ready, B-14).
   2. **The IC/Motion tab animation feature.**
   3. **Aesthetics redo** (color scheme mostly).
   4. **Deeper functionality / capability / value-prop coverage.**
   5. **Per-node self-serve demo videos** — *eventual requirement, explicitly NOT a launch gate* (may launch before these exist).

2. **Current canon versions** (`_SYSTEM/CANON-INDEX.md`, RATIFIED at W2): `site-skeleton.md` **v0.3** · `design-directions.md` **v0.3 ("Control Room")** · `brand-voice.md` **v0.4**. **⚠️ Build far past canon — DRAFT amendments A-15 → A-25 pending** (spirits-first voice, Proof-as-craft, pricing analogies, 70s-Retro pivot A-19, outcome-first copy, About-as-whole-family, AMS wheel, the "AI" page + nav IA, the buried founders page). **Ratify + bump at the operator merge gate; Doer does not self-ratify.** Note: launch item 3 (aesthetics redo) will likely amend design canon *again* — batch with A-15..A-25.

3. **THE SITE IS LIVE (coming-soon), unchanged this round.** `slowboilproductions.com` → Vercel → the coming-soon holding page ("I hear the train a-comin'…" + the Johnny Cash photo, IP flagged, operator's risk) with the Google-Ads compliance set inline (LLC name · Charlotte NC · Privacy/Terms/Contact · HTTPS · responsive). Inner pages on the holding build stay `noindex`'d (they keep it — holding-branch only).

4. **Branch map (after this round):**
   - **`production-holding`** — NEW; the durable holding branch, = `claude/busy-hamilton-jqwkgt` tip `f2e2f5d`, byte-identical. **Vercel production repoints here (operator, §O-guided). NEVER merges to `main` — protected by convention.**
   - **`claude/sbp-os-production-holding-h3l1ks`** — this round's build branch: main-restoration (revert of PR #4 `8bdcaab`) + W9 close docs (`f2e2f5d` cherry-pick) + screenshot slots (`8e2708d` cherry-pick) + this STATE/B-24. **Inert until PR merge; merge only after the Vercel repoint is verified.**
   - **`main`** — still the PR #4-polluted tree until the restoration PR merges; after merge = the full 13-page W8 site + W9 docs + slots, fully indexable, launch-ready-pending-gate-list.
   - `claude/fervent-davinci-uch9ig` — the W8 recovery point (kept; the revert restored its exact tree) · `claude/busy-hamilton-jqwkgt` — superseded by `production-holding` once Vercel repoints (leave until then) · `fallback-1` @ `57f5363` — frozen pre-motion base.

5. **Infra (per brain-os Infra Registry — B-5 resolved):** production = **Vercel** (project `sbp-os`); DNS = **Namecheap** (apex `A` → `216.198.79.1`, `www` CNAME → Vercel). **Launch = a Vercel production-branch flip, NO DNS change.** Cutover loose ends still OPEN (**B-22**): ⚑ `www` go-green/forward-to-apex unverified since W9 close (operator browser-check owed) · remove the domain from **Netlify** (cleanup, deliberately last).

6. **The full site (post-restoration `main`):** Home · The Stack · 4 subunits (Marketing · CRM · Operational Dashboard · Image Creation) · AI · Proof · About (+ buried Founders, noindex by design) · Contact · Privacy · Terms — 13 pages, 70s-Retro, spirits-first, no pricing, "Operational Dashboard" never "Dash", BC nowhere, Charlotte NC. **Motion layer COMPLETE + audited** (W6/W7). **Screenshot slots swap-ready** on CRM (slot) / Operational Dashboard (featured) / IC + Marketing ("coming") — awaiting JCV screenshots (B-14/B-23). **IC showcase still asset-blocked** (Phase A Sunshine Punch turntable; Phases B/C brand-LoRA).

7. **Open items** (`_SYSTEM/CAPTURE-QUEUE.md`): **B-24** this round (backlog + hygiene; gates open) · **B-22** cutover loose ends (www + Netlify) · **B-23** JCV demo brand (cross-unit, Foreman pickup) · **B-14** screenshot slots awaiting images · **B-13** partially actioned (deepen CRM/OD when Brain delivers) · **B-12** BLOCKED on operator definition · **B-10** parked (recorded demo) · **B-4** Ads-token status unconfirmed · **A-10** Indian-Trail watch-item · **B-21** W5 close-docs orphan on `exciting-rubin` (non-blocking) · audit nits **N1/N2** carried. **A-15..A-25** DRAFT — ratify at the merge gate.

8. **Last round:** SBP-W9 — `_SYSTEM/Session_Logs/2026-06-18_SBP-W9_SCL.md` (lands on `main` with this round's PR). This round's SCL + COST row: at the operator-triggered close, post-audit (Doer does not self-close).

9. **Next actions / gates:** **(1)** operator repoints Vercel production → `production-holding` (§O one-step-at-a-time; live site verified identical before/after) · **(2)** operator-triggered audit of this round's branch · **(3)** PR merge restores `main` (also ratifies A-15..A-25 if the operator so rules) · **(4)** then the launch gate list (§1) drives the remaining rounds; JCV cross-unit session is UP NEXT family-side · **(5)** B-22 loose ends (www check, Netlify removal). **Process locks:** Doer does not kick off audits or self-close · no popups (B-6) · §O for any live/infra step · **never merge `production-holding` (or busy-hamilton) into `main`.**
