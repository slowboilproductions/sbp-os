# STATE — sbp-os (generated snapshot)

**As-of:** 2026-06-17 close · round **SBP-W8** (the new top-level **"AI" page** — the full How-We-Use-AI walk + per-node deep-links — **plus** the flagged-forward **"Meet the Founders" easter egg**) · _build branch `claude/fervent-davinci-uch9ig` (off `main` @ `21e879d`, the merged W3–W7 site); **operator-triggered §K Opus audit = PASS-WITH-NITS, no blockers**; **PR to `main` opened at close** (operator asked for the SCL + PR)._ **Inert — nothing newly merged, nothing new on `slowboilproductions.com`.** `main` = `21e879d` (W3–W7 / PR #2). Pre-motion base frozen on `fallback-1` @ `57f5363`.

1. **Current canon versions** (`_SYSTEM/CANON-INDEX.md`, RATIFIED at W2): `site-skeleton.md` **v0.3** · `design-directions.md` **v0.3 ("Control Room")** · `brand-voice.md` **v0.4**. **⚠️ The build has moved far past canon — DRAFT amendments A-15 → A-25 pending:** spirits-first voice, Proof-as-craft, pricing analogies, the **70s-Retro pivot (A-19, supersedes "Control Room")**, outcome-first copy (A-20), About-as-whole-family (A-21), voice tweaks (A-22), the AMS wheel (A-23), **the new "AI" page + nav IA (A-24)**, and **the buried "Meet the Founders" page (A-25)**. Canon docs still read pre-pivot (design-directions still says "Control Room"; skeleton §2 nav predates the AI tab + founders slot; §3.5 predates the wheel). **Ratify + bump at the operator merge gate; Doer does not self-ratify.** Flag, don't action.

2. **W8 result:** **(a)** New top-level **`/ai` page** (`ai.astro`) — present-tense (W8(a)), **walled/private framing led up front** (W8(b)); intro/thesis → **four node anchors whose slugs match the stack slugs** (`#marketing`/`#crm`/`#operational-dashboard`/`#image-creation`, each with a back-link) → the **`#brain` climax** (run >1 node → a Brain reasons ACROSS all) → an on-page **Trust section** ("not a ChatGPT wrapper"). This is the **B-13 memory-engine story, built in**; honesty line **A-17** held. **(b)** Nav "AI" tab placed **between The Stack and Proof** (desktop + mobile). **(c)** Four **"How we use AI here →"** `.btn--mono` deep-links, one per stack hero. **(d)** Buried **"Meet the Founders" easter egg** (`/founders`) — off-nav, **`noindex`** (new `BaseLayout` prop), reachable only from a magnetic `.btn--primary` at the bottom of `/about`; two cofounders **Brian + "V"** (the AI), operator's own verbatim-ish script + two gag portraits extracted from his `.docx`. **(e)** Operator-triggered **§K Opus** audit = **PASS-WITH-NITS, no blockers.**

3. **The site (as built):** Home · The Stack · 4 equal subunits (**Marketing · CRM · Operational Dashboard · Image Creation**) · **AI** (new) · Proof · About (+ buried **Founders** egg) · Contact (compliance) · Privacy · Terms. **70s-Retro skin** · spirits/bev-alc first · no rate card (analogies OK) · "Operational Dashboard" never "Dash" · BC nowhere · Charlotte NC · chocolate-ink temp logo · **13 pages green.**

4. **Motion layer status:** **COMPLETE + audited** (W6/W7). Site-wide `SiteMotion` + `SiteDrift` + `PageWipe`; per-unit signatures (AMS wheel · CRM chaos→intelligence · OD converge · IC `BottleStudio`). The new `/ai` + `/founders` pages add **no new islands** — they reuse the established safe pattern (`[data-reveal]` + magnetic `.btn--primary`; reduced-motion / no-JS safe). **Load-bearing safety pattern** held throughout.

5. **⭐ IC showcase — still asset-blocked (unchanged):** **Phase A** (360 turntable) = real studio shots, operator shooting **Sunshine Punch** → drop frames in `public/ic-360/`, set `FRAMES`. **Phases B & C** = **LoRA-generated** stills + motion clip → `stills`/`motionSrc`. Swap-only into `BottleStudio` scaffolding.

6. **Open items** (`_SYSTEM/CAPTURE-QUEUE.md`): **A-15..A-25** DRAFT (ratify at gate) · **A-24** AI page+nav IA · **A-25** founders easter egg · **B-13** PARTIALLY ACTIONED (memory-engine story built into `/ai`; can deepen CRM/OD when Brain delivers) · **B-12** control-state competitor-intel **BLOCKED on operator definition** (distinct from the built CRM social-scrubbing) · **B-10** parked (CTAs→email; confirm `hello@` routing at Step-3) · **B-11** stood down · **B-4** Ads-token (pre-cutover) · **B-5** hosting (Step-3; Vercel = preview only) · **A-10** Indian-Trail-vs-Charlotte watch-item · **B-21** W5 close-docs orphan on `exciting-rubin` (non-blocking). **Audit nits N1 (/ai "knows knows") / N2 (founders README in public) carried** (operator chose to land as-is).

7. **Audit:** **RUN this round — OPERATOR-TRIGGERED** (the Doer does NOT self-initiate audits; corrected mid-session). Independent distinct-instance **Opus** subagent over `origin/main...HEAD`. **PASS-WITH-NITS, no blockers.** Report: `_SYSTEM/Session_Logs/2026-06-17_SBP-W8_Audit-Report.md`. COST-REGISTER W8 row appended.

8. **Branch / main:** `main` = **`21e879d`** (W3–W7 / PR #2) · build branch **`claude/fervent-davinci-uch9ig`** (all W8; inert) · **PR to `main` open** (operator asked for it this close) · **`fallback-1`** @ `57f5363` (frozen pre-motion 70s base).

9. **Last round:** SBP-W8 — `_SYSTEM/Session_Logs/2026-06-17_SBP-W8_SCL.md`; cold handoff `_SYSTEM/Cold_Handoffs/2026-06-17_SBP-W8_CCH.md`; audit `_SYSTEM/Session_Logs/2026-06-17_SBP-W8_Audit-Report.md`.

10. **Next action / gates:** operator **preview-tuning** (`/ai` + `/founders`, built preview-blind) → sweep nits N1/N2 if wanted → **deepen CRM/OD** with Brain memory-engine content (B-13) → **finish IC** on assets → **merge** (operator ratifies A-15..A-25 with the PR) → **Step-3 deploy gate** (B-5 host + B-4 Ads-token; §O cutover — site never goes dark, compliance always present). **Process lock:** the Doer does not kick off audits or self-close — the operator triggers those.
