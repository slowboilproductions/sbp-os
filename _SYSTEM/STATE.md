# STATE — sbp-os (generated snapshot)

**As-of:** 2026-06-15 close · round **SBP-W3** (Step-2 build + live preview + voice rewrites + logo) · _build branch `claude/sbp-os-site-build-step2-4ljx5x` @ `a4e6758`, 9 commits ahead of `main` (`625ab9a`); **inert — nothing merged, nothing deployed to `slowboilproductions.com`**_

1. **Current canon versions** (`_SYSTEM/CANON-INDEX.md`, all RATIFIED at W2): `site-skeleton.md` **v0.3** · `design-directions.md` **v0.3** (Direction **A "Control Room"**) · `brand-voice.md` **v0.4** · Conventions pointer DRAFT. **⚠️ DRAFT amendments pending (A-15..A-18):** the *site is built to* spirits-first voice, bev/alc-loud, Proof-as-craft, and restored pricing analogies — but the **canon docs still read the pre-feedback framing** (skeleton §3.7 metrics; brand-voice §1 broad-CPG, §4 no-pricing). These ratify + bump **at the operator merge gate**. Doer builds to the live ruling, does **not** self-ratify. Flag, don't action.

2. **W3 result:** built the full site in `site/` (Astro static, Control Room system, 11 pages, compliance set baked into every page) → stood up an **operator-navigable Vercel preview** (guided; real domain untouched) → **3 operator-feedback rewrite passes** to his first-person, spirits-first voice (his own pasted copy) → wired his **temp logo**. **Paused (by the operator) on the feel/look + website-features call** — the motion layer.

3. **The site (as built):** Home · The Stack overview · 4 equal subunits (**Marketing · CRM · Operational Dashboard · Image Creation**) · **Proof** (= proof of *craft* — website/CRM/dashboard/photography/video/Meta campaign — **not** lift metrics; SSP/DGT/MWC loose framing, no ownership) · About (operator's first-person origin story) · Contact (custom form UI; handler = Step-3) · Privacy · Terms. **Spirits/bev-alc first** (CPG = eventual TAM) · **no rate card** (pricing *analogies* allowed as voice, A-18; actual quote → Contact) · **"Operational Dashboard"** never "Dash" · **BC nowhere** · Charlotte NC · logo (temp) in nav + footer.

4. **Open items** (`_SYSTEM/CAPTURE-QUEUE.md`): **A-15..A-18** DRAFT canon amendments (ratify at the gate) · **B-7** motion-feature backlog (the operator's current decision) · **B-4** Ads dev-token *status* (re-confirm pre-cutover) · **B-5** hosting (Step-3 gate; Vercel = preview only) · **A-10 watch-item** (Indian Trail vs Charlotte) · **B-8** logo CONFIRMED (temp) · **B-9** Drive/brand-bible **DROPPED** (connector unusable; operator's call).

5. **Live preview:** operator's Vercel project `sbp-os` auto-builds this branch each push; `vercel.json` (repo root) builds `site/`→`site/dist`, host-agnostic (keeps B-5 open). Use the newest deployment / branch-alias URL (per-deployment URLs are immutable).

6. **Audit:** **not run this round** (build inert). The §K independent distinct-instance audit runs **next session, before the merge ask**; **COST-REGISTER row deferred** to land with it (§N: after the audit).

7. **Branch / main:** `main` = **`625ab9a`** (W2 ratification — canon only, no `site/`); build branch **`claude/sbp-os-site-build-step2-4ljx5x`** = **`a4e6758`** (all W3 work; inert).

8. **Last round:** SBP-W3 — `_SYSTEM/Session_Logs/2026-06-15_SBP-W3_SCL.md`; cold handoff `_SYSTEM/Cold_Handoffs/2026-06-15_SBP-W3_CCH.md`. (W2 audit report remains the latest audit on file.)

9. **Next action:** operator returns with the **feel/look + motion-features** decision → build the **motion layer** (B-7) → **spawn the independent audit** → **merge ask** (ratify A-15..A-18 with the build) → then **Step 3 deploy** (B-5 + B-4; §O cutover — site never goes dark, compliance always present).

10. **Open gates:** the **operator merge gate** for the Step-2 build (after the motion layer + audit) is the next gate; the **Step-3 deploy gate** follows.
