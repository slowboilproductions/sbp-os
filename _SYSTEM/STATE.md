# STATE — sbp-os (generated snapshot)

**As-of:** 2026-06-18 close · round **SBP-W9** (deploy/cutover — **the coming-soon holding page is LIVE** and production was cut **Netlify → Vercel**; Step-3 partially executed, §O, operator-guided). **`slowboilproductions.com` is now live on Vercel serving the coming-soon, with HTTPS + the full compliance set.** Holding branch `claude/busy-hamilton-jqwkgt`. **No audit this round** (none triggered). The full W8 site is untouched on `claude/fervent-davinci-uch9ig`. ⚠️ **`main` is polluted** — PR #4 merged the coming-soon into it (see §8).

1. **Current canon versions** (`_SYSTEM/CANON-INDEX.md`, RATIFIED at W2): `site-skeleton.md` **v0.3** · `design-directions.md` **v0.3 ("Control Room")** · `brand-voice.md` **v0.4**. **⚠️ Build is far past canon — DRAFT amendments A-15 → A-25 pending** (spirits-first voice, Proof-as-craft, pricing analogies, 70s-Retro pivot A-19 superseding "Control Room", outcome-first copy, About-as-whole-family, AMS wheel, the "AI" page + nav IA, the buried founders page). **Ratify + bump at the operator merge gate; Doer does not self-ratify.** Flag, don't action.

2. **THE SITE IS LIVE (coming-soon).** First time the real domain serves the new infra. `slowboilproductions.com` → Vercel → the **coming-soon holding page**: **"I hear the train a-comin'…"** + a Johnny Cash photo (operator's call; **IP flagged**, his risk), brand-skinned, with the **Google-Ads compliance set inline** (Slow Boil Productions LLC · Charlotte NC · Privacy/Terms/Contact · HTTPS · responsive). Built via a new `BaseLayout` `bare` prop; image is swap-ready (`/public/coming-soon/hero.png`). Unfinished pages on this branch are `noindex`'d.

3. **Cutover (B-5 RESOLVED — production = Vercel; DNS = Namecheap).** Was on **Netlify**; DNS managed at **Namecheap** (BasicDNS). This round: Vercel production branch → the holding branch (promoted); domain added in Vercel (bare-domain primary, www→307→apex); **Namecheap** apex `A @` `75.2.60.5`→**`216.198.79.1`**, `www CNAME`→`a3370b9d28dc4877.vercel-dns-017.com`. Apex propagated + Valid + HTTPS, operator-confirmed. **Never went dark.** **Launch later = a Vercel production-branch flip, NO DNS change ever again.** Vercel: project `sbp-os`, team *brian-thomason-s-projects*, auto-assign prod domains ON; `crm`/`dash` subdomains already on Vercel.

4. **Two cutover loose ends OPEN** (B-22): **(a) www** — confirm green in Vercel + forwards to apex (was propagating at close); **(b) Netlify cleanup** — remove the domain from Netlify so nothing dangles (left for last so www never blinks).

5. **Demo brand = "Johnny Cash Vodka" (JCV)** (B-23) for all CRM + Dashboard demo data — **no real brand/client data on the public site** (operator). He wants **REAL screenshots of the actual Dash + CRM**, which **the website cannot produce** — JCV must be loaded **into those products** → a **cross-unit session (SBP + Dash + CRM + brain)** the **Foreman will pick up.** SBP's part: pre-build a JCV demo-brand identity + data pack + dummy logo to seed it, then drop the screenshots into the site's ready slots (CRM table, dashboard panels — currently hand-built mockups, not screenshots). **IP flag** carried on the celebrity name for the permanent site.

6. **The full site (the "future build") = `claude/fervent-davinci-uch9ig`** (W8: Home · The Stack · 4 subunits · AI · Proof · About · buried Founders · Contact · Privacy · Terms; 70s-Retro; 13 pages). Preview-only, untouched this round. **IC showcase still asset-blocked** — Phase A (Sunshine Punch real turntable frames) + Phases B & C (brand-LoRA stills + motion) → swap into `BottleStudio`.

7. **Motion layer:** COMPLETE + audited (W6/W7), unaffected this round.

8. **⚠️ `main` is polluted.** PR **#4** merged `claude/busy-hamilton-jqwkgt` (coming-soon) into `main`, so **main's homepage is the coming-soon + inner pages noindex'd** — `main` is **NOT** the live full site. The full build is safe on `claude/fervent-davinci-uch9ig`. **At launch: restore the real full-site homepage + drop the holding noindex on the launch branch, THEN flip Vercel production.** **Do NOT merge the holding branch into `main`.**

9. **Branches:** `claude/busy-hamilton-jqwkgt` = the **production/holding branch** (Vercel tracks it; coming-soon + this round's commits; **never merge to main**) · `claude/fervent-davinci-uch9ig` = full W8 site (preview) · `main` = polluted by PR #4 · `fallback-1` @ `57f5363` = frozen pre-motion 70s base.

10. **Open items** (`_SYSTEM/CAPTURE-QUEUE.md`): **B-5** RESOLVED (Vercel + Namecheap) · **B-22** coming-soon live + cutover loose ends · **B-23** JCV demo brand (cross-unit pickup) · **B-13** partially actioned (memory-engine story in `/ai`) · **B-12** control-state competitor-intel BLOCKED on operator definition · **B-10** parked · **B-11** stood down · **B-4** Ads-token (treat domain as possibly-live) · **A-10** Indian-Trail-vs-Charlotte watch-item · **B-6 reinforced** (no popups — conversation only). Audit nits **N1/N2** still carried (on the full-site branch).

11. **Last round:** SBP-W9 — `_SYSTEM/Session_Logs/2026-06-18_SBP-W9_SCL.md`. (No audit report; none triggered. No COST-REGISTER row — appended post-audit only.)

12. **Next actions / gates (Foreman pickup):** **(1)** JCV demo-brand cross-unit session (SBP pre-builds identity + data pack + logo → Dash/CRM load it → screenshots → SBP places them) · **(2)** finish IC visuals (assets) · **(3)** close the www + Netlify loose ends · **(4)** at launch: clean `main` / restore full-site front, then flip Vercel production (no DNS) · **(5)** ratify A-15..A-25 at the merge gate. **Process locks:** Doer does not kick off audits or self-close; no popups (B-6); §O for any live/infra step (one verified step at a time, never dark, compliance always present).
