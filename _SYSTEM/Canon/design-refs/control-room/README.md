# Design Reference — Direction A "Control Room" (SELECTED 2026-06-13)

This folder preserves the **agreed visual starting point** for the SBP site, picked at the operator's W2 design gate (2026-06-13). It is the reference the full design system builds out from at Step 2.

## Files
- `control-room_home-mock.html` — self-contained home-page hero + even four-card stack, in the Control Room system (dark `#0D1117` ground, signal-green `#3FB950` accent, Space Grotesk / IBM Plex Sans / IBM Plex Mono). Responsive (desktop + mobile breakpoint at 760px).
- `control-room_home_desktop.png` — desktop render (1280w).
- `control-room_home_mobile.png` — mobile render (390w).

## Important — what this is and is NOT
- **Illustrative, not canon copy.** Headlines/sublines are voice *candidates* from `brand-voice.md` §6; the dashboard numbers are placeholders. No site copy is ratified here (no-content-before-gate holds).
- **Not the build.** This is a reference mock, not the site source. The real build lands in `site/` at Step 2.
- **Starting point, not frozen.** Operator flagged he may still tweak; refinements happen in the Step-2 design-system pass.
- Reflects skeleton **v0.3** structure: one operational stack, four equal subunits (Marketing · CRM · Operational Dashboard · Image Creation), all shown "live" per ruling A-12.

Renders were produced with a throwaway headless Chrome inside the session's ephemeral container (HTML → PNG); nothing was installed on the operator's environment or touched the live site.
