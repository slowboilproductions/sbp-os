# COLD HANDOFF — 2026-06-17 · SBP-W7 → next SBP build session

**One-line state:** All four units now **lead in the operator's first-person voice** AND each carries a **distinct signature feature** (AMS "Marketing Stack" wheel · CRM chaos→intelligence pipeline · OD 8-sources→one-dashboard converge · IC bottle-studio), plus a tricolor **page-wipe** and a **site-wide warm drift** backdrop. **§K Opus audit = PASS-WITH-NITS, no blockers.** Nothing newly merged; nothing on the real domain. **PR to `main` opened at close** (operator's merge ask). Next: operator preview-tuning → sweep nits if wanted → build the B-13 memory-engine story when Brain delivers → finish IC on assets → merge + Step-3 deploy.

## Branch / build state
- Work branch: **`claude/brave-noether-hmqw9a`** (forked from the W6 tip `eed7145`). All inert. `git fetch` and continue here (or fork fresh from it).
- `main` = **`9778bb9`** (W4); **W6 motion base is unmerged**, carried inside this branch; **`fallback-1`** @ **`57f5363`** = pre-motion 70s base (don't push to it).
- `npm --prefix site run build` → **11 pages green.** Live preview = operator's Vercel `sbp-os` (branch alias / newest deployment). **Doer has no Vercel access** — can't watch builds; the operator checks the dashboard. If a branch shows no build, the project may still be pointed at an older branch (guide a repoint, §O).
- **PR**: opened to `main` this close (head `claude/brave-noether-hmqw9a`). Contains the W6 motion base + all W7 work.

## What W7 built (all on the binding guardrails; §K PASS-WITH-NITS)
- **PageWipe.astro** (BaseLayout) — tricolor diagonal wipe on client-side nav; JS off Astro lifecycle; `transition:persist`; `pointer-events:none`/`aria-hidden`/transient. KEPT. (Banner sway built then CUT — Banner is static.)
- **SiteDrift.astro** (BaseLayout) — the warm blob/grid/stripes drift, **site-wide** at Marketing's old intensity; content lifted to z-1 via `.main-content`; local marketing drift removed.
- **AmsStackWheel.astro** (marketing) — scroll-formed exploding 7-slice "Marketing Stack" pie + steel "Think Tank" center hub; hover/tap captions; legend kept in DOM (hidden when JS on).
- **CrmChaos.astro** (crm) — 24 nuggets funnel into the CRM, then an intelligence pipeline draws OUT (3 branches → Catch stockouts / Measure lift / Replicate success), gated to start only once the noise funnels away.
- **DashConverge.astro** (operational-dashboard, under the hero) — 8 disconnected feeds wire into one live dashboard.
- **Four hero rewrites** in operator voice (Marketing placed-on/pulled-off pain block + "fought for the reorder"; CRM "nothing sexy about a CRM"; OD "Excel spreadsheet jockey").

**THE LOAD-BEARING SAFETY PATTERN (keep for any new motion):** every feature is armed only after a `prefers-reduced-motion` early-return / a JS-added class; SVG visual layers are `aria-hidden` with meaning carried by real text (sr-only / legend / chips); transform/opacity-only, rAF-throttled, no scroll-jack. **Pipe/wire hiding = OPACITY, never `stroke-dashoffset` on a stretched `non-scaling-stroke` SVG (that doesn't hide — it shows faint dashes; W7 bug, fixed).**

## Pick up here (NEXT ACTION)
1. **Operator preview reaction / tuning** (built preview-blind): wheel slice sizing · explode distance · assemble timing · steel hub; CrmChaos density + funnel pace; DashConverge chip layout + wiring pace; the new site-wide drift on non-Marketing pages.
2. **Sweep audit nits N1/N2/N4** if the operator wants the cleaner record (carried this round at his call): N1 wheel legend keyboard a11y (make slices focusable or drop dead focus path) · N2 remove scroll listeners on `astro:before-swap` across the four islands · N4 make CrmChaos flow-keyframe disable explicit like DashConverge.
3. **B-13 — build the memory-engine differentiation story** into CRM/Dashboard once the Brain Foreman delivers the content. Honesty line **A-17**: describe the capability, never promise client results/lift.
4. **Finish the IC showcase** when real assets land (Phase A Sunshine Punch frames → `public/ic-360/`, set `FRAMES`; Phases B & C on the brand LoRA → `stills`/`motionSrc`). Swap-only.

## ⚠️ Canon-lags-build (flag, don't action — Doer does not self-ratify)
Site is built to operator rulings **A-15 → A-23** (CAPTURE-QUEUE Register A, DRAFT). Canon docs still read pre-pivot — `design-directions.md` v0.3 still says "Control Room"; skeleton §3.5 (Marketing) predates the wheel. These ratify and bump **at the operator merge gate**.

## Open items (CAPTURE-QUEUE)
- **A-15..A-23** DRAFT → ratify at the merge gate. **A-23** = the AMS wheel + the two-competitor-intels split.
- **B-13** ADDED — memory-engine site story, pending Brain content (above).
- **B-12** control-state competitor-intel — **BLOCKED on operator definition** (DISTINCT from the CRM social-scrubbing, which is built). Write NO copy until defined.
- **B-10** recorded-demo PARKED; CTAs → `mailto:hello@slowboilproductions.com` (confirm routing at Step-3). **B-11** stood down. **B-4** Ads-token (pre-cutover) · **B-5** hosting (Step-3; Vercel = preview only) · **A-10** Indian-Trail-vs-Charlotte watch-item.

## After the build / before live
The §K W7 audit is **done (PASS-WITH-NITS)** and logged. The **merge** (operator ratifies A-15..A-23 with the build — PR is open) and the **Step-3 deploy** (B-5 host + B-4 Ads-token; §O cutover — site never goes dark, compliance always present) remain operator gates. Re-run a fresh distinct-instance audit over any new changes before the next merge ask.

## Gotchas / lessons
- **Doer is preview-blind** (no in-container browser, no Vercel access) — motion/visuals verified by the operator on Vercel; build clean, expect a tuning pass.
- **Operator is web + mobile only**; asset hand-off is GitHub web upload or the swap slots. **No popups — propose in plain chat (B-6).**
- **Stretched-SVG stroke can't be hidden by dashoffset+non-scaling-stroke+pathLength** — gate by opacity (cost a round-trip when the operator caught pipes showing during chaos).
- Each scroll island uses a single `querySelector` — fine for one-per-page; generalize to `querySelectorAll` if ever placed twice.
- §K default auditor is Fable when the Doer is Opus; **this round the operator explicitly requested Opus** — honor the operator's call.
