# COLD HANDOFF — 2026-06-17 · SBP-W8 → next SBP build session

**One-line state:** Built the new top-level **`/ai` page** (the full How-We-Use-AI walk: intro/thesis → four node anchors → the **Brain-node climax** → an on-page Trust section), wired **"How we use AI here →" deep-links** from all four stack heroes, and — as a bonus, the flagged-forward "About fun idea" — buried a **"Meet the Founders" easter egg** at `/founders` (Brian + the AI cofounder "V", off-nav + `noindex`, operator's own script + gag portraits). **Operator-triggered Opus §K audit = PASS-WITH-NITS, no blockers.** **PR to `main` opened at close** (operator asked for the SCL + PR). Nothing merged/live yet.

## Branch / build state
- Work branch: **`claude/fervent-davinci-uch9ig`** (off `main` @ `21e879d`; the merged W3–W7 site). Tip at close ≈ `d38ad97` + the close-docs commit. All inert.
- `main` = **`21e879d`** (PR #2 / W3–W7). **`fallback-1`** @ **`57f5363`** = pre-motion 70s base (don't push to it).
- `npm --prefix site run build` → **13 pages green** (adds `/ai` + `/founders`). Live preview = operator's Vercel; **Doer is preview-blind** (no Vercel access) — expect a tuning pass.
- **PR**: opened to `main` this close (head `claude/fervent-davinci-uch9ig`).

## What W8 built (on the binding guardrails; operator-triggered Opus audit PASS-WITH-NITS)
- **`ai.astro`** (NEW) — present-tense (W8(a)), **walled/private framing led up front** (W8(b)); four node sections whose **slugs match the stack slugs** (`#marketing`/`#crm`/`#operational-dashboard`/`#image-creation`), each with a back-link; the **`#brain` climax**; an on-page Trust section ("not a ChatGPT wrapper"). This is the **B-13 memory-engine story, built in**. Honesty line **A-17** held.
- **`Nav.astro`** — "AI" tab between "The Stack" and "Proof" (desktop + mobile).
- **Four stack heroes** — a `.btn--mono` "How we use AI here →" deep-link each.
- **`founders.astro`** (NEW) + **`BaseLayout` `noindex` prop** + `/public/founders/` portraits + the About-page `.btn--primary` egg button — the buried two-cofounder page.

**SAFETY PATTERN (unchanged, keep for any new motion):** reduced-motion early-return / no-JS static fallback; decorative layers `aria-hidden`; transform/opacity-only; no scroll-jack. `/ai` and `/founders` use only `[data-reveal]` + the magnetic `.btn--primary` (both via `SiteMotion`, which early-returns under reduced-motion / no-JS).

## Pick up here (NEXT ACTION)
1. **Operator preview reaction / tuning** on `/ai` (walled lead, node copy, Brain climax, Trust cards) and `/founders` (portrait crop, captions, alternating rows).
2. **The About-tab "fun idea" is now BUILT** (the founders egg) — if the operator wants V's bio fleshed out or her own voice, that's a quick copy pass.
3. **Sweep audit nits if wanted (carried this round):** **N1** — `/ai` Brain line doubled word "knows knows" (`ai.astro`, one-word fix). **N2** — `site/public/founders/README.md` publishes to `dist/founders/README.md`; relocate out of `public/` if the operator dislikes shipping the swap note.
4. **Deepen CRM + OD node copy** with Brain-delivered memory-engine content when it lands (B-13; honesty A-17).
5. **Finish the IC showcase** on real assets (Phase A Sunshine Punch frames → `public/ic-360/`, set `FRAMES`; Phases B & C on the brand LoRA → `stills`/`motionSrc`). Swap-only.

## ⚠️ Canon-lags-build (flag, don't action — Doer does not self-ratify)
Site is built to operator rulings **A-15 → A-25** (CAPTURE-QUEUE Register A, DRAFT). Canon docs still read pre-pivot — `design-directions.md` v0.3 still says "Control Room"; skeleton §3.5 (Marketing) predates the wheel; **skeleton §2 (nav) predates both the AI tab (A-24) and the founders slot (A-25)**. These ratify + bump **at the operator merge gate**.

## Open items (CAPTURE-QUEUE)
- **A-24 / A-25** DRAFT → ratify at the merge gate (AI page+nav; founders easter egg).
- **B-13** PARTIALLY ACTIONED — memory-engine story built into `/ai`; can deepen CRM/OD when Brain delivers.
- **B-12** control-state competitor-intel — BLOCKED on operator definition (distinct from the built CRM social-scrubbing). Write NO copy until defined.
- **B-10** recorded-demo PARKED; CTAs → `mailto:hello@slowboilproductions.com` (confirm routing at Step-3). **B-11** stood down. **B-4** Ads-token (pre-cutover) · **B-5** hosting (Step-3; Vercel = preview only) · **A-10** Indian-Trail-vs-Charlotte watch-item. **B-21** W5 close-docs orphan on `exciting-rubin` (non-blocking).

## After the build / before live
The §K W8 audit is **done (operator-triggered Opus, PASS-WITH-NITS)** and logged. The **merge** (operator ratifies A-15..A-25 with the build — PR open) and the **Step-3 deploy** (B-5 host + B-4 Ads-token; §O cutover — site never goes dark, compliance always present) remain operator gates. Re-run a fresh distinct-instance audit over any new changes before the next merge ask.

## Gotchas / lessons
- **The Doer does NOT kick off audits — the operator triggers them.** (Corrected mid-session: a Doer-initiated audit launch was the wrong call; wait for the operator's explicit "spin up an auditor.") Same for self-closing.
- **Doer is preview-blind** (no in-container browser, no Vercel access) — visuals verified by the operator on Vercel; build clean, expect tuning.
- **Operator is web + mobile only**; content/asset hand-off is GitHub web upload or a pasted file. The founders portraits arrived **embedded in a `.docx`** — extractable in-container (zip → `word/media/`), no separate upload needed.
- **`noindex` on `BaseLayout`** is opt-in (default false) for buried pages; it does **not** strip the compliance footer (still inherited).
- §K default auditor is Fable when the Doer is Opus, **but Fable 5 is currently unavailable** — substitute a distinct Opus instance (also the operator's standing preference).
