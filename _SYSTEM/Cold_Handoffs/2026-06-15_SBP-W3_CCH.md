# COLD HANDOFF — 2026-06-15 · SBP-W3 → next SBP build session

**One-line state:** Step-2 site is **built, on a live Vercel preview, and rewritten to the operator's voice** — paused (by the operator) while he decides the **feel/look + website features** (the motion layer). Nothing merged, nothing deployed to the real domain.

## Pick up here (next action)
The operator returns with his **motion/feature decision**. Then:
1. Build the **motion layer** on top of the static site (markup already carries `data-anim` hooks). Triage **B-7** (parallax · multi-layer / z-axis / 3D scroll · tunnel-zoom) + hero boot-up + scroll choreography against the Control Room system and the binding constraints (fast static · WCAG-AA · `prefers-reduced-motion` · no popups · compliance set never obscured).
2. **Spawn the independent audit** (§K, distinct instance) over the whole `site/` build + amendments A-15..A-18.
3. Bring the **merge ask** — the operator ratifies canon amendments **A-15..A-18** at the same gate as the build merge.
4. Then **Step 3 (deploy)** at its own gate: B-5 hosting + B-4 Ads-token; §O cutover discipline.

## Branch / build state
- Work branch: **`claude/sbp-os-site-build-step2-4ljx5x`**, tip **`a4e6758`**, 9 commits ahead of `main` (**`625ab9a`**). All inert — `git fetch` and continue on this branch; **do not** branch off a stale ancestor.
- `site/` = Astro static (`output: 'static'`); `vercel.json` at repo root builds `site/` → `site/dist` (host-agnostic; keeps B-5 open). `npm --prefix site run build` → 11 pages, green.
- **Live preview:** operator's Vercel project `sbp-os` (his "Brian Thomason's projects" team) auto-builds this branch on every push. Per-deployment URLs are immutable — use the **newest** deployment or the **branch alias** (Domains tab). `slowboilproductions.com` untouched.

## What's built (skeleton v0.3, Control Room, operator voice)
Home · The Stack overview · 4 subunits (Marketing / CRM / Operational Dashboard / Image Creation) · Proof · About · Contact · Privacy · Terms. Compliance set on every page by construction. Custom Contact **form UI** (handler is a Step-3 host-coupled hookup — never a secret in source). Logo (operator's **temp**) wired: pot icon → nav, full lockup → footer.

## Hard constraints (still binding — verify on every change)
BC **nowhere** · **"Operational Dashboard"** public, **"Dash" never** · spirits/bev-alc **first** (CPG = eventual TAM) · pricing **analogies** allowed as voice (A-18: "$20k / half the cost / $50k / $500k") but **an actual quote routes to Contact** (no rate card) · Charlotte NC · compliance set (privacy · terms · contact · business name · HTTPS · mobile-responsive) on every page.

## ⚠️ Canon-lags-build (flag, don't action)
Site is built to operator rulings **A-15..A-18** (CAPTURE-QUEUE Register A, DRAFT). The canon docs still read pre-feedback: **skeleton v0.3 §3.7** frames Proof as metrics (now proof-of-craft, A-17); **brand-voice v0.4 §4** says no-pricing (now relaxed, A-18); §1 broad-CPG (now spirits-first, A-15). These ratify — and the canon docs bump — **at the operator merge gate**, not before. Doer does not self-ratify.

## Open items (CAPTURE-QUEUE)
- **A-15..A-18** — DRAFT canon amendments, pending operator ratification at the gate.
- **B-4** Google-Ads dev-token *status* — re-confirm before the Step-3 cutover.
- **B-5** hosting — Step-3 deploy gate (Vercel adopted for *preview* only; production host + DNS still the operator's call).
- **B-7** motion-feature backlog — the decision the operator is now taking.
- **A-10 watch-item** — flag if the live Google-Ads account lists **Indian Trail** vs **Charlotte**.
- **B-8** logo — CONFIRMED (temp; swap on a final). **B-9** Drive/brand-bible — **DROPPED** (operator: "can the drive"; connector hard-denies at the managed-env layer; voice grounded in the operator's pasted copy).

## Gotchas / lessons
- Drive MCP is **unusable** in this env (server-side "requires approval" regardless of local allow-list) — don't burn time on it; have the operator **paste** any Drive content.
- Operator is **web + mobile only** — file handoff is **GitHub web upload** (worked for the logo) or paste; guide Vercel one verified step at a time (§O).
- Screenshot-render loop is the main token cost — batch pages per render call; build/render/image tooling is Bash/binary (no model tokens).
