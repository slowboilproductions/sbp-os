# /screens/marketing — Marketing "in action" shots

Renders on `/stack/marketing` (the "Marketing in action" section). Until files
land it shows an honest **"in action, coming"** placeholder — **do not mock or
fabricate** shots here; this is for real work once it exists. See `../README.md`
for specs and the no-real-data hard lock.

Candidates when ready: a shipped campaign, a built site, a content calendar, an
analytics report (scrubbed). Lowercase-dashed names; prefix `01-`, `02-` to order.

- `.webp` preferred (`.png` ok) · ~1600px long edge · **< 250 KB** (400 KB max).
- **Demo / illustrative or scrubbed data only** — no real client names or figures.
