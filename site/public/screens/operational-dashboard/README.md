# /screens/operational-dashboard — Operational Dashboard screenshots

Renders on `/stack/operational-dashboard` in the **featured** slot (operator's
strongest single asset — given prominent placement right after the problem
lands, ahead of the supporting motion showcase). Empty → dashed placeholder
slot. See `../README.md` for specs and the no-real-data hard lock.

**Drop shots here.** Suggested lead: the one-screen summary front page; then
depletions / state tabs, load-in & AR, open orders & inventory. Lowercase-
dashed names (e.g. `summary-front-page.webp`); prefix `01-`, `02-` to order.

- `.webp` preferred (`.png` ok) · ~1600px long edge · **< 250 KB** (400 KB max).
- **Demo / illustrative data only** — no real account names, outlets,
  customers, $ figures, or distributor names.
- **Public name is "Operational Dashboard," never "Dash."** If the UI chrome in
  a shot says "Dash," flag it to the operator — do not publish the mismatch.
