# /screens/image-creation — Image Creation "in action" shots

Renders on `/stack/image-creation` (under "Still and motion, from one engine").
Until files land it shows an honest **"in action, coming"** placeholder — **do
not mock or fabricate** shots here. The real assets are the brand-trained
stills/motion (Phase A studio frames → `public/ic-360/`; Phases B & C LoRA
output) — those feed `BottleStudio`; this slot is for finished
before/after or sample stills once they exist. See `../README.md` for specs.

- `.webp` preferred (`.png` ok) · ~1600px long edge · **< 250 KB** (400 KB max).
- Brand-true imagery only; nothing that surfaces real client data.
